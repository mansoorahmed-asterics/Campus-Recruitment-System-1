import Type from "../const/Types";
const initState = {

}
const AdminReducer = (state = initState, action) => {
    switch(action.type){
        default:
        return state;
    }
}
export default AdminReducer;